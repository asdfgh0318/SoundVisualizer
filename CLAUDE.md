# SoundVisualizer

Drone-noise directivity measurement tool. Captures audio from up to 6 miniDSP UMIK-2 mics arranged on a vertical arc plus thrust/torque/current telemetry from a Tyto Robotics 1585 stand, computes per-mic FFT and elevation-polar SPL plots, optionally adds Norsonic NOR-145 SLM data.

## Status

**Refactor MVP is built.** Phases 0/1/2/3/4/5/7/8/9/10 of [PLAN.md](PLAN.md) are complete. Server + frontend run end-to-end against either real hardware (when `tyto.enabled = true` in `config.toml`) or against synthesised "fake capture" data for results-tool development without the rig.

## Next session — start here (written 2026-09-08)

Read `docs/arc-validation-remedies.pdf` §00 (nine lines) and §15 (the campaign plan), then:

1. **Get the missing geometry from Adam** and put it in `docs/mic_arc.md`: hub height above the floor, room L×W×H, foam material/thickness/coverage, what stands within 1.5 m of the −72°/−90° and +36°/+54° microphones, how far below the propeller disc the Tyto beam sits and on which side. Known: ring diameter 1.68 m (r ≈ 0.84 m); the propeller blew upward, so the stand is in the inflow. Then re-run the plane fit in `scripts/arc_validation_diagnostics.py` with the real numbers.
2. **Loudspeaker session (arc flat)** — source options researched and priced in `docs/hub-source-options.md` (borrow the B&K OmniSource 4295 from PW Zaklad Elektroakustyki, or Avantone MixCube 1222 zl, or a Visaton FRS 8 M in a 1 l sealed box for ~250 zl; dodecahedrons are 25-39 cm and too large for the hub). No sweep generator, playback path or deconvolution script exists yet — build them before the session. The run: 20 Hz–20 kHz sine sweep from the hub, all 11 mics, motor off; each mic's own impulse response gives its echo delays (no clock sync needed); then tones at 216/238/258 Hz. Decides room-vs-inflow for the 238 Hz pattern (report Problems 1, 7, 8) and names the 0.7 m and 1.5 m reflectors. Needs the small sealed loudspeaker Adam is arranging.
3. **Speed ladder with cyclic mic rotation:** 12–15 PWM steps 1750–2050, 2 s each, BPF read from the audio; shift every mic one spot per run over 11 runs; one run with reversed rotation. Report §15 sessions 3–4.
4. **Software before those runs — done 2026-09-09** (issues #12, #13): `bpf_hz` in `meta.json`, per-harmonic tones and tone-notched bands in `/fft`, `off_speed` flags in `pwm_points`, Polar level-source selector; trigger sync no longer crops a running source (that was the short PWM-1800 WAVs). Left: a low-voltage flag from the telemetry, and re-running the September keys through the new pipeline to confirm the maps (`scripts/arc_validation_diagnostics.py` remains the reference).
5. **Any new PDF Adam drops** (they land in `papers/anechoic-simulation/` or `papers/arc-validation/`): identify from front matter, rename `Author_Year_Venue_slug`, `pdftotext -q` into `txt/`, extract with page-referenced quotes under `papers/arc-validation/PROTOCOL.md`, run `scripts/verify_quotes.py`, update the report chapter and Appendix B/C, commit in the **papers** repo (private), push. Fetch list: report Appendix C (35 items; ISO/IEC are paywalled at PW, skip them).
6. ~~Re-check `docs/reflection-localization.html` against Sun 2012 / Mabande 2013~~ — done 2026-09-09: 5/5 claims verified, no edits (`papers/reflection-localization/VERIFICATION-2026-09-09.md`).

Remaining phases:
- **Phase 6 — Norsonic NOR-145**: hardware pending delivery. Paweł's `norsonic*.py` is vendored and dormant in `server/vendor/pawel/`.

Production deployment (Phase 10) targets a **Raspberry Pi 5** (running Raspberry Pi OS (Debian 13 trixie base), hostname `sound-viz`, reachable at `sound-viz.local:8000`; user `pi`, password `dupa`). Deploy from the laptop with `scripts/deploy_to_pi.sh pi@sound-viz.local` (builds the bundle locally, ships it, runs `scripts/setup_rpi.sh` on the Pi). Native systemd (`deploy/soundvis.service`), **system Python 3.13** (Trixie ships it; all deps have aarch64 wheels, so no pyenv/compile), FastAPI-served bundle on :8000. See [deploy/README.md](deploy/README.md). The instance is live. (Reflashed 2026-07-22 after a second SD-card death — previous identity was hostname `jama`, user `jama`. The laptop SSH aliases `jama` / `soundvis-pi` / `sound-viz` all point at the new identity.) **Immutable-rootfs design (live since 2026-07-24):** the SD card is partitioned `boot 512M | rootfs 4.5G | data 2.4G (ext4, LABEL=data)`. The rootfs runs under **overlayroot** (`overlayroot=tmpfs:recurse=0` in `/boot/firmware/cmdline.txt`) — all rootfs writes go to RAM and vanish on reboot, so SD corruption (which killed two cards) is impossible. The **data partition is the only persistent storage**: mounted at `/home/pi/data`, with `~/SoundVisualizer/data → /home/pi/data/measurements`, `~/SoundVisualizer-data → /home/pi/data/backup-repo`, `~/SoundVisualizer/config.toml → /home/pi/data/config.toml`, and `~/{duct,drone-paczek}-research-tree → /home/pi/data/research-trees/…` symlinks — so measurements, the GitHub backup clone, the server config (incl. Tyto calibration) AND both research trees survive reboots and are editable without the overlay ceremony. Swap is zram-only (`/etc/rpi/swap.conf.d/99-zram-only.conf`; the 2G `/var/swap` file was removed to fit the shrunk rootfs).
  - **To change anything persistent** (packages, udev, systemd units), edit `/boot/firmware/cmdline.txt` directly and reboot:
    - off → `overlayroot=disabled` (reboot; `/` becomes `/dev/mmcblk0p2 … ext4 (rw)`), make the change,
    - on → `overlayroot=tmpfs:recurse=0` (reboot).
    The boot partition is never covered by the overlay, so this edit needs no ceremony — it's a one-line `sudo sed -i`.
  - **`raspi-config nonint do_overlayfs` DOES NOT WORK on this Pi** (verified 2026-08-28). It exits 0 and changes nothing. This machine uses the **`overlayroot` Debian package** driven by the kernel parameter; raspi-config manages a *different* initramfs `boot=overlay` mechanism that is not in play here. `/etc/overlayroot.conf` sets `overlayroot=""`, so the cmdline parameter is the only switch. Ignore the old raspi-config instructions.
  - **CRITICAL gotcha:** never write plain `overlayroot=tmpfs` — always `:recurse=0`. Without it overlayroot stacks a RAM overlay over the data partition too (mounts it ro underneath → measurements silently become ephemeral). Verify after every toggle with `mount | grep /home/pi/data` — it must show `/dev/mmcblk0p3 … ext4 (rw)`, not an overlay.
  - Config lives on the data partition, so **`config.toml` edits need no overlay toggle at all.**
  - Cutoffs + tare are runtime state and reset every boot regardless of overlay — re-push cutoffs before any powered run.

Reference docs: [PLAN.md](PLAN.md) · [2.md](2.md) (original scope) · [1.md](1.md) (kickoff) · [docs/overview.html](docs/overview.html) · [docs/recirculation-plan.html](docs/recirculation-plan.html) (why the ascending-PWM sequence lets a wake build over the whole run, and what to change) · [docs/hub-source-onepager.html](docs/hub-source-onepager.html) (one page on the hub loudspeaker; detail in `docs/hub-source-options.md`) · [docs/anechoic-simulation.html](docs/anechoic-simulation.html) (chamber-simulation literature review; one-page summary in `docs/anechoic-simulation-onepager.pdf`) · [docs/reflection-localization.html](docs/reflection-localization.html) (how to locate a reflection once a chamber fails qualification) · [docs/anechoic-chamber-problems-brief.pdf](docs/anechoic-chamber-problems-brief.pdf) (6-page brief: how chamber problems — standing waves, interference, recirculation, shear layers — show up in data, how to read them, what people do; sources in `papers/chamber-problems/`, HTML source alongside the PDF). [docs/chamber-story.pdf](docs/chamber-story.pdf) (the narrative: what the 69 held chamber documents say when read in order, every number quoted from a held and audited source; start here) · [docs/chamber-literature-synthesis.pdf](docs/chamber-literature-synthesis.pdf) (deep-research lit-review over all 69 held sources: graded evidence matrix, convergence per theme, cross-paper tension inventory, gaps, graded recommendations for the arc rig; matrix rows with quotes in `papers/*/MATRIX-3W.md`). **Where the papers live (since 2026-09-08):** the whole library (PDFs, text dumps, extracts, audits) is the private repo `github.com/asdfgh0318/papers`, cloned at `~/ŻYCIE/PRACA/papers`; `papers/` in this repo and `docs/references/` are symlinks into it (`../papers/SoundVisualizer`, `../../papers/SoundVisualizer/references`), so every `papers/...` path in the docs resolves. New PDFs and extracts are committed **there**, never here (this repo is public and the PDFs are paywalled). On a fresh device: `gh repo clone asdfgh0318/papers ~/ŻYCIE/PRACA/papers` and recreate the two symlinks (recipe in that repo's README). **Paper hygiene (2026-09-07):** every PDF under `papers/` is named `Author_Year_Venue_slug` from its own front matter (`papers/BIBLIOGRAPHY.md`); every claim in the extracts and docs was audited against the source text by five Sonnet auditors (`papers/AUDIT-1…5.md`) and the 27 corrections are logged in `papers/ERRATA.md`. Two files worth knowing: the Beranek document held is the 1945 OSRD report (Beranek, Sleeper & Moots), not the 1946 JASA paper; NASA CR-3371 is by Schlinker & Amiet (Dec 1980). `pdftotext -layout` interleaves two-column papers, so verify quotes against a plain `pdftotext` extraction. The core chamber papers are full text in `papers/anechoic-simulation/` and `papers/reflection-localization/`; `docs/anechoic-simulation.html` and its one-pager were re-checked claim by claim against them on 2026-09-07 (one correction: the round-robin numbers on card 12 were not in Brinkmann and were withdrawn; verification log in `papers/anechoic-simulation/VERIFICATION-2026-09-07.md`). The same re-check of `docs/reflection-localization.html` against Sun 2012 / Mabande 2013 was done on 2026-09-09 (5/5 verified, `papers/reflection-localization/VERIFICATION-2026-09-09.md`). The academic-research-skills repo (`~/academic-research-skills`) is symlinked into `~/.claude/skills/` (deep-research, academic-paper, academic-paper-reviewer, academic-pipeline); its `source_verification_agent` protocol is the one used for these re-checks. **Chamber-landscape campaign (2026-09-11):** seven parallel threads (`papers/chamber-landscape/PROTOCOL.md`, EXTRACTS/RETRIEVAL-L,M,O,P,Q,R,INSTEAD) built a world picture of how rotor noise is measured in rooms that are not chambers: 29 named facilities in 17 countries, qualification practice, the alternatives to a chamber, the low-frequency wall, rig/inflow effects, low-cost builds, and the standards landscape; 35 new PDFs. Output: **`docs/chamber-fighting-guide.pdf`** (11 pp., the methods menu with lowest usable frequency / residual error / cost / failure mode per method, and our programme in order). All 94 literature quotes in it were verified verbatim against the text dumps. Headline findings: cut-offs cluster at 63-275 Hz regardless of room size (depth buys cut-off, not volume); time gating is limited to ~500 Hz in our room by the 1.98 ms echo; a measured per-position room correction reached <0.55 dB above 200 Hz for a non-conforming fan facility (du Plessis 2022) and is admissible for our FIXED arc though not for a flying drone (Rasmussen 2022); CIRA runs the SAME Tyto 1585 stand and found its pylon shifts directivity up to 4 dB, revealed by reversing rotation; the rotor literature almost never tests azimuthal symmetry (it assumes it), so our flat-arc result is unusual rather than wrong; Mehrgou 2012 found loudspeaker and real engine disagreeing in a chamber "especially not in the regions of 200 and 250 Hz", which is a caution on the loudspeaker-substitution plan. **Arc-validation literature campaign (2026-09-08):** nine parallel threads under `papers/arc-validation/PROTOCOL.md` (every held paper re-read from a fresh plain `pdftotext` dump and diffed against the earlier notes, ~45 new PDFs, extracts `EXTRACTS-A…I.md`, retrieval logs `RETRIEVAL-A…I.md`); `scripts/verify_quotes.py` checks quoted passages mechanically against the text dumps (alphanumeric-key match, so OCR spacing does not cause misses). Corrections it found to the earlier notes are listed in the remedies report Appendix A (page slips only; Rajmane & Baumann's 35 cm plate registers from 1000 Hz, not 1250 Hz). Adam's standing rule: re-read the PDF, do not trust an earlier agent's extract; papers that cannot be fetched go on a precise list for his Politechnika Warszawska library proxy.

## Hardware

| Device | Status | Notes |
|--------|--------|-------|
| 6× UMIK-2 USB mics | Available | Plug into laptop USB hub. Setup page filters audio devices to ALSA `(hw:…)` only. |
| Tyto Robotics 1585 thrust stand | Available | USB serial, driven via Paweł's MSP protocol code. |
| Norsonic NOR-145 SLM | Pending delivery | WiFi when delivered; phase 6. |
| Raspberry Pi 5 | Available, **deployed & live** | Raspberry Pi OS (Debian 13 trixie base), hostname `sound-viz` → `http://sound-viz.local:8000`. Native systemd service running on system Python 3.13. Deploy/update from the laptop via `scripts/deploy_to_pi.sh pi@sound-viz.local`. SSH alias `ssh jama` (passwordless key; also `ssh sound-viz`). (Replaced the originally-planned RPi 4; the Pi 5's RP1 chip has real USB 3.0, so no VL805 bandwidth workaround.) **Integration-tested on the Pi** with 4× UMIK-2 + Tyto: mics enumerate + capture, Tyto on `/dev/ttyUSB0` connects + streams telemetry + cutoff watchdog + full capture run + tare all verified. UMIK-2s report USB serial `00000`, so `generate_udev.py` names them by **USB port path** (`umik_3_1_4_1`…) and the device picker shows that `alsa_card_id`; the live level meter is the physical-identify tool. Temps unused. ESC/battery not yet connected (no powered/prop run done). **Power lesson:** run mics off a self-powered USB hub — hanging everything off the Pi's ports once brown-outed it and corrupted the SD card (recovered by reflash + `setup_rpi.sh`). |

Laptop is Linux (kernel 6.8). Same Python server runs on the RPi 5 with no code change — only the host moves.

## Running it

**Always launch the backend against the real measurement repo.** `SOUNDVIS_DATA` must point at
`../SoundVisualizer-data/data` (the GitHub backup clone, 35+ real bases). Without it the store
falls back to `./data`, which holds only junk/seed test keys — Adam does not want to see those
(standing instruction, 2026-09-15). `git pull` the data repo first when it is reachable.

```bash
# Backend — SOUNDVIS_DATA is mandatory
git -C ../SoundVisualizer-data pull --ff-only   # skip if offline / no SSH key
SOUNDVIS_DATA=../SoundVisualizer-data/data .venv/bin/uvicorn server.main:app --reload --port 8000

# Frontend (separate terminal)
npm run dev
# → http://localhost:5173

# Populate demo data without hardware (the wizard's fake-capture button was removed 2026-09-11, issue #17;
# /dev/seed and /dev/fake_capture stay as curl-only dev endpoints)
curl -X POST http://localhost:8000/dev/seed
```

Tests: `.venv/bin/pytest server/tests/` (122 passing). Lint: `.venv/bin/ruff check server/ scripts/`.

Demo via Docker (no install needed): `docker compose up --build` → http://localhost:8000. **The `--build` is not optional after a pull**: `docker-compose.yml` pins `image: soundvisualizer:latest`, so plain `docker compose up` reuses whatever image of that name already exists and serves a stale bundle with no warning (this bit Paweł on 2026-09-15 — he pulled, ran `docker compose up`, and got a build predating the Intro page). Multi-stage Dockerfile bundles the React build into FastAPI's static mount; no hardware passthrough.

## Locked decisions

- **No azimuth measurement.** Tested objects are rotationally symmetric around the vertical axis. Elevation is the only varying spatial dimension. The 360° polar plot is a cosmetic mirror of the measured arc.
- **Single-pass capture is the default.** Each mic has one absolute elevation (−90…+90°) and all configured mics record simultaneously in one run; `MeasurementHalf.FULL` labels the resulting measurements. The backend still accepts `half="top"`/`"bottom"` (legacy + future two-pass clients), so existing two-pass data renders unchanged; the wizard just doesn't drive that flow today.
- **Filesystem JSON+WAV store**, no SQL. Keys are `motor__propeller__shroud__notes` directories under `data/`. Per-measurement layout: `meta.json` + `audio.wav` (acoustic) or `telemetry.csv` (performance) or `norsonic.{txt,json}` (NOR data — later).
- **Tyto serial port: use the stable by-id path**, not `/dev/ttyUSBn`. A replug re-enumerates the FTDI to a new node while the running service keeps the dead handle — the poll loop stalls silently (`connected: true` stays stale, telemetry stops, and PWM stops being transmitted since it rides on each poll). `config.toml` uses `tty = "/dev/serial/by-id/usb-FTDI_FT231X_USB_UART_D30F775I-if00-port0"`. Symptom to recognise: "Tyto not outputting PWM" + empty `/tyto/ws/telemetry`.
- **Thrust calibration verified against a 200.0 g reference (2026-07-24):** Paweł's `cal_thrust` read 4.3% low on our rig; corrected iteratively to `cal_thrust = -1.0019910071` (final check: 200.30 g read for a 200.00 g weight, +0.15%). Lives in `[tyto.calibration]` of the Pi's `config.toml` (on the data partition). Method: tare empty → place reference weight → read absolute; iterate the scale factor.
- **Paweł Sadowski's [ars_noise_measurement](https://git.swarozyn.pl/mtj/ars_noise_measurement.git) code is ours** to copy/edit (full permission, 2026-05-06). Vendored in `server/vendor/pawel/` (`msp.py`, `async_serial.py`, `thrust_stand.py` for Tyto; `norsonic*.py` for NOR-145). Calibration constants in `thrust_stand.py` are *overridden at runtime* from `config.toml` via `server/core/calibration_override.py`.
- **Cutoff triggers:** all eight (current, voltage, RPM, thrust, torque, temp0, temp1, temp2) — each tickbox-enabled with a numeric threshold and an above/below direction. Server-side watchdog reads `PollResponse` at ~33 Hz and slams `mot_pwm = 1000` on any trip; latched until reset.
- **Auto-tare per run:** the orchestrator re-zeros the stand once at the top of every capture run, while the motor is still at idle (PWM=1000). Eliminates load-cell drift bleeding into thrust/torque/current readings. Not re-taring between PWM steps — that'd force a spool-down/up per step and isn't worth the wear/time. Manual `POST /tyto/zero` still works for ad-hoc taring outside of a run.
- **Tyto serial auto-reconnect:** the FT231X USB link drops and re-enumerates on its own (seen on the rig, 2026-07-27). The service treats "poll raised" *or* "no sample within 1 s" as a dead link, tears it down and reopens `config.tyto.tty` with escalating backoff (0.5/1/2/5 s, forever). PWM always comes back at **1000** — a reconnect never resumes a live throttle. Tare offsets, cutoff config and the trip latch survive. `GET /tyto/status` reports live state via `link_state` (`absent` = not configured, `connected`, `reconnecting`) and the telemetry WS emits a `{connected: false, link_error}` frame on the drop. A capture run in flight is **failed** (not silently continued) — without the poll stream there's no telemetry, no PWM transmission and no cutoff watchdog. Because `MSPSlave.open_connection` blocks the event loop forever waiting for the board's `Ready` banner, the open handshake lives in `thrust_stand_service.open_stand` (bounded, run in a thread) instead of the vendored one.
- **Multi-tree research-tree integration:** SoundVis supports any number of [research-tree](https://github.com/asdfgh0318/duct-research-tree)-style editors as companion services. Configured via repeated `[[research_trees]]` tables in `config.toml`. On the Pi: [`duct-research-tree`](https://github.com/asdfgh0318/duct-research-tree) on `:8123` (single-duct work), [`drone-paczek-research-tree`](https://github.com/asdfgh0318/drone-paczek-research-tree) on `:8124` (parcel-drone material studies). Node IDs are unique across trees by prefix (`p1-*` for duct, `dp1-*` for drone-paczek), which is how the backend routes a capture-completion push to the right tree. The legacy singular `[research_tree]` table still loads (folds into a one-entry list). Adding more trees later: clone the repo, append a `[[research_trees]]` entry + a systemd unit (see `scripts/setup_rpi.sh`'s `RESEARCH_TREES` array). **On the Pi both clones live on the data partition** at `/home/pi/data/research-trees/<name>`, symlinked from `~` (moved 2026-08-28) — before that they sat on the overlay rootfs and every tree edit was silently lost on reboot. Put new trees there too. dp1 materials so far: baseline, EPP, Thinsulate, cardboard, XPS, felt, rubber, felt+rubber, cork.
- **Measurement-data backup to GitHub — driven from the laptop since 2026-09-02.** `scripts/backup_from_laptop.sh` rsyncs the Pi's `/home/pi/data/measurements/` into the local `../SoundVisualizer-data` clone, commits, pushes; a **user systemd timer on the laptop** (`~/.config/systemd/user/soundvis-backup.timer`, every 15 min) polls for it and exits quietly when the Pi is unreachable or a capture is in flight. It is **additive (no `--delete`)** so pruning old campaigns off the Pi never removes them from the backup, and `flock`-serialised so timer and manual runs cannot collide. **Why not on the Pi:** the old Pi-side mirror (`scripts/backup_data.sh`, `soundvis-backup.timer`) keeps measurements ×2 — a working copy plus git objects, and WAVs do not compress (37 commits, 593 MB reachable from HEAD = 593 MB reachable from all history; shallowing frees nothing) — which filled the 2.3 GB data partition mid-session on 2026-09-02. The Pi script is still deployed but **exits immediately while the marker `/home/pi/data/backup.laptop-managed` exists** (data partition, so it survives reboots and the `setup_rpi.sh` re-enable). Remove the marker to hand backups back to the Pi once it has more disk. Auth on the laptop is the normal GitHub credentials; the Pi's deploy key is unused meanwhile. Introduced after losing all measurements to a corrupted SD card on 2026-06-25 — one SD failure should never again take data with it.
- **Arc validation (Sept 2026 "horizontal" runs, arc laid flat):** one-page A4 chamber findings `docs/arc-validation-onepager.pdf` (rev 3, 2026-09-17; start here); interactive guide `docs/arc-validation-guide.html` (self-contained; data pack `docs/analysis/arc-validation-data.json`); **remedies report `docs/arc-validation-remedies.pdf`** (16 pp., rev 2026-09-17, one chapter per problem with a literature answer and a verdict, the next-campaign plan and the questions for Adam; HTML source alongside). With the arc horizontal and the prop axis vertical every mic should read the same; `scripts/arc_error_map.py` fits run gain + position pattern + mic offset over many runs (`docs/analysis/arc-error-map*.{png,csv}`), and `scripts/arc_validation_diagnostics.py` extends it to all five PWM steps with tones and broadband separated, standard errors, a single-echo comb per position and a cepstrum by speed (`docs/analysis/remedies-*`, log in `remedies-diagnostics.txt`). **What the data say (2026-09-08):** the room bends the *blade tones* by up to 8 dB but the tone-notched *broadband* polar is round to ±1.3 dB in 84 % of cells from 125 Hz to 8 kHz (worst ±3.3 dB, **re-fitted 2026-09-17 with the measured capsule corrections and no mic term**; it read 92 % / 2.7 dB when the fit still had to estimate the microphones) — that residual is real, not scatter (90 of 209 cells exceed three standard errors) and is no smaller above 3 kHz than below, so **the arc is not qualified at any frequency yet**; "broadband works from 125 Hz" is a two-error picture, not a clean sheet; the tone pattern changes completely between BPF 216, 238 and 258 Hz (−36° −8.3 dB at 238 Hz, −72° −7.8 dB at 258 Hz, round at 216 Hz) and the broadband floor 7–15 Hz beside the tone is flat, so the 200–260 Hz error is either narrow room modes sampled by a line spectrum or a propeller whose inflow is not axisymmetric (NASA's 1970s static-fan literature shows a support structure does exactly that) — only a loudspeaker at the hub decides; two fixed reflectors are real (extra path 1.5 m with a hard-surface amplitude at +36°/+54°, 0.7 m at −72°), which is the 630–1000 Hz band; mic **811-1892 reads 2–6 dB low below 2.5 kHz** (s.e. 0.2–0.4 dB; Sens Factor −10.63 dB does not match the unit) — **re-calibrated 2026-09-16, not retired**: the substitution session measured it −3.60 dB against the group and found its capsule flat to sd 0.24 dB across the band, so only the certificate was wrong; the other ten are within ±2 dB. Bookkeeping faults found: prop7 spun at BPF 164 Hz where the others spun at 238 Hz because the bench supply was current-limiting (8 V in its telemetry against 12 V elsewhere; exclude it, and check the voltage column of every run), the PWM-1800 WAVs are 0.2–1.8 s long in nine runs, PWM 1200 makes no tone, `telemetry.csv` has rpm = 0 (BPF must be read from the audio), and the earlier cepstrum figure `prop18-pwm1900-floor-ripple.png` shows the propeller's harmonics (its peaks scale with 1/BPF), not echoes. Runs prop11–14 had the arc turned 180° (label +e physically at −e); prop10→11 swapped mics 811-1892 and 810-8901. **Next campaign, in order** (report §15): record the geometry; loudspeaker sweep at the hub with all 11 mics (no clock sync needed, each mic's own impulse response gives its echo delays) plus tones at 216/238/258 Hz; a 12–15-step motor-speed ladder with a cyclic mic rotation across runs; treat or move what the sweep names; the same sweep with the arc vertical; a calibrator session. Geometry known so far (in `docs/mic_arc.md`): ring diameter 1.68 m (r ≈ 0.84 m); in the horizontal runs the propeller blew upward away from the stand, so the stand sits in its inflow. Hub height, room size, foam and what stands near the arc ends are still unrecorded — record them before anything else.
- **Substitution calibration done — all eleven capsules, 2026-09-16.** Every capsule measured against every other at one seat, 12 tones/octave, amplitude 0.03, 55 pairs (`calibrator/sessions/2026-09-16/`; levels/meta preserved in the data repo under `data/calibrator-sessions/`, WAVs local only). **The loudspeaker is deliberately not a source of truth**: each capture is `L_i(f) = S(f) + M_i(f)`, so differencing two capsules at one seat cancels source, room and analog chain exactly — `S` need only be *stationary*, and the closing bracket proved it was (reference repeated to −0.025 dB mean over 2 h 13 min, sd 0.165). Repeatability 0.082 dB sd, 0.008 dB for points above −45 dBFS. **Seven of eleven factory files were wrong by 0.6–3.6 dB**, clustered by serial prefix rather than scattered (810-8897/8901/8904 all at +1.02…+1.04 dB across three separate certificates; both deep negatives are 811-18xx — suspect a miniDSP batch systematic). Corroborated twice: r = +0.87 against the arc fit's own per-mic offsets, and r = +0.966 between the corrections applied and how far the refitted arc moved each mic. **Datum = the four capsules whose files agree with each other AND with measurement** (810-8900, 810-8903, 811-1896, 811-2321) — not the group mean, because batch-correlated error makes the mean biased; note the +1.0 dB group is the *tighter* cluster (0.02 dB) so this is a defensible choice, not a measurement. Corrections went entirely into `cal.gain_db` (offset and shape), **Sens Factors kept verbatim**, held flat outside 90 Hz–16 kHz; originals in `data/calibrations/factory-originals-2026-09-16/`; re-runnable via `calibrator/apply_corrections.py`. Acceptance: spread across the eleven in the SPL they report fell **4.01 dB → 0.02 dB**. After correction the arc fit's mic terms fell from 1.16 to 0.85 dB RMS and their spread 4.55 → 2.63 dB, while **the position map moved only 0.21 dB** with the mic term still estimated; dropping the mic term entirely (2026-09-17) moves it 0.45 dB mean / 3.13 dB worst and costs only 0.14 dB of residual (1.43 → 1.56), which is the acceptance test. −36° vs +36° at 250 Hz now reads **11.7 dB** (was 12.5). What remains open: the **absolute** anchor — this method is blind to any error common to all eleven, and only a 94 dB calibrator closes it (ask PW Zakład Elektroakustyki alongside the OmniSource). The correction is **normal-incidence only**, which matches the arc. 810-8904 is a poor permanent reference (file +1.04 dB out *and* a real ~1 dB dip at 2–8 kHz); use 810-8900 or 811-1896.
- **Self-audit (2026-09-11).** `docs/analysis/audit_2026_09_11.py` recomputes every number the one-pager, the remedies report, the guide and this file assert from our own data and checks it against what is printed: **40 of 40 pass**, after one correction (a 9.3 dB contrast was labelled broadband when it came from the mixed-band fit; the broadband value for that pair at 794 Hz is 2.87 dB, and the largest broadband within-group contrast is 4.3 dB at 397 Hz). Write-up `docs/analysis/AUDIT-2026-09-11.md`. **Re-run the script after editing any of those documents.** Literature quotations are checked separately by alphanumeric-key match against the text dumps (101/101 in the guide).
- **Whole-arc stepped tones — `calibrator/rig.py`, `python -m calibrator rig` (2026-09-17, untested on hardware).** The 2026-09-16 substitution session was run **with the chamber cleared: one capsule, one speaker, nothing else** (Adam, 2026-09-17), so by construction it cannot see anything the assembled rig does to a capsule. That matters because eleven bodies on the 1.68 m ring sit **26.3 cm apart** (chord, 18°), and half a wavelength equals that spacing at **653 Hz** — while the capsule-swap audit leaves its largest unexplained residual at **630 Hz, −2.58 dB**. Array self-scattering is therefore a live hypothesis for part of what the position map calls "room", and it is fixed to position (each spot has a different neighbourhood), so it lands in P and is indistinguishable from room error in every existing capture. `rig.py` plays a steady tone from the hub through the calibrator's inline PortAudio generator while `server.core.capture.capture_simultaneous` records **all eleven capsules at once** — the library, not `POST /capture/acoustic`, which would write 11 measurements into the data repo per tone. Identity comes from a Setup preset (`alsa_card_id` is the stable udev port-path name; PortAudio indices shift per boot) and a missing capsule is a hard error listing all eleven. `assert_arc_ok` gates every capsule's own `Mic Capture Volume` at unity before a tone is played: one capsule left at −6 dB reads as a −6 dB error at its POSITION. A gate failure is recorded per capsule, not fatal for the frequency — with eleven mics, one bad channel must not discard ten good readings. **Identity is the weak link, and `python -m calibrator identify --preset …` is the check:** a UMIK-2 reports USB serial `00000`, so which capsule sits on a given cable cannot be verified electronically, and nothing in the system knows which arc position a cable ends at — `load_arc` only proves which USB *port* each stream comes from. With the source at the hub every capsule is equidistant, so the measurement itself carries no positional information either. `identify` is a live per-capsule meter (level, peak-hold, `<<< TAP` when a channel jumps >12 dB above its own slow baseline): tap a capsule, watch which row moves. Nothing is inferred and nothing is written. Run it before the first rig capture — a swapped pair would produce exactly the position-fixed anomaly the experiment is hunting for. **The decisive experiment:** `rig full-rig --preset … --fmin 500 --fmax 4000 --per-octave 24` (72 tones, ~5 min, resolves a comb down to 0.96 ms), then pull the two capsules either side of one position and repeat. What changes is the array; what stays is the room. **Mains is not a problem for tone work (measured 2026-09-17):** battery vs mains changed tone levels by 0.017 dB mean / 0.036 dB sd / 0.16 dB worst, while the noise floor's odd 50 Hz harmonics rose 11–27 dB (150 Hz −93.4 → −66.0 dBFS, 350 Hz −96.2 → −70.3; even harmonics barely moved — a switching supply, not a ground loop). A line at −66 dBFS under a −30 dBFS tone adds 0.001 dB. Two caveats: quiet bands below ~250 Hz in *propeller* captures can sit near those lines, and the default 24/octave grid anchored at 1 kHz puts 10 of its 73 tones within 3 Hz of a 50 Hz harmonic — offset the grid (`--fmin 503 --fmax 4001`) so none coincides. Stepped tones can recover a single reflection's delay from the magnitude comb alone (`comb_fit` already does this), so a sweep is only needed for *several* overlapping arrivals and for time gating — and a sweep across independent USB clocks (PCM2902 out, UMIK-2 in) slips 0.5 ms over 10 s, so keep any sweep to 1–2 s.
- **Flipping the arc splits the position map — but it does not split it in half (measured 2026-09-17, corrected same day).** With the arc flat, the printed 1 l sphere at the hub firing up the symmetry axis (so every capsule sees the same polar angle and source directivity cancels), 112 tones 257–6350 Hz at 24/octave over all eleven capsules at once: the position pattern spans **3.86 dB** and repeats to **0.026 dB rms** run to run (worst cell 0.20 dB) — an extraordinarily quiet measurement. Adam then **physically flipped the arc** and it was repeated. **The per-capsule calibrations are applied** (`rig.py` records `level_db_spl` at capture time from `data/calibrations/`, and `rig.read_map()` is the single loader every plot and analysis goes through, so a figure can never disagree with the capture about what a level means). Uncalibrated dBFS gives materially different and *wrong* answers — the spread was 4.88 dB raw, and an earlier version of this entry claimed the whole broadband per-position offset was arc hardware; that was an artefact of the missing calibration and is **withdrawn**. **How to combine the two runs (Adam, 2026-09-17).** Both runs are indexed by *label* and a capsule keeps its label through the flip, so `A_p = X_p + Y_p` and `C_p = X_p + Y_m(p)` with `X` travelling with the capsule/clamp/cable/frame, `Y` staying in the room and `m` = mirror. **Un-flip the second run before averaging**: `rig = (A + C)/2 = X + Y_sym` and `room = (A + mirror C)/2 = Y + X_sym`, which shows each subject *whole* — all of the room's asymmetry lands in the room panel (0.87 and 1.34 dB rms). **Do not plot `(A − C)/2` and call it the room**: that is only `Y`'s antisymmetric half, and it looks antisymmetric because the arithmetic made it so, not because the room is — the first version of this figure did exactly that and both panels came out looking mirror-symmetric, which is what gave it away. **What two runs can and cannot settle:** of `X_sym, X_anti, Y_sym, Y_anti` the flip determines three — **`X_anti` = 0.21 dB rms provably rig**, **`Y_anti` = 1.04 dB rms provably room**, and **`X_sym + Y_sym` = 0.84 dB rms shared with no way to divide it**. Breaking that last tie needs a different move on the room (move the source, or take the arc off the symmetry plane), not another flip. The frequency-averaged room term is −0.14…+0.18 dB per position, i.e. the room acts through frequency structure (strongest 251–500 Hz) rather than a broadband per-position offset. **Consequences:** (a) the rig's own contribution is small — 0.21 dB, not the ~1 dB first reported; (b) `X` cannot separate capsule from clamp from frame, since all three flip together; (c) the ~1 dB the capsule-swap audit could not explain is mostly in the ambiguous term and is still unattributed. **Serials verified against the capsule bodies (Adam, 2026-09-17)** — every UMIK reports USB serial `00000`, so nothing electronic can check it and this was done by reading labels. **Today's map does not correlate with September's** (r = −0.04 tone-notched, +0.04 mixed band) and no relabelling rescues it — all 22 orientations/rotations tested, best r = +0.24, against +0.79…+0.98 when `detect_rotated()` finds a genuine flip. Explanations still open: the propeller's own non-axisymmetric field, the 137 mm sphere replacing the propeller/motor as the object at the hub, or the room having changed since 2 September. Sessions in `calibrator/sessions/2026-09-17/` (levels+meta mirrored to the data repo); plot with `calibrator/plots/plot_rig.py <run> <out.png>` and `calibrator/plots/plot_flip_split.py`.
- **THE SPHERE IS NOT AN OMNI SOURCE ABOVE ~3 kHz — and the arc is nearly innocent (vertical session, 2026-09-17).** The arc was remounted **vertical** (its normal orientation) with the printed 1 l sphere at the ring centre firing along the symmetry axis, and the same grid re-run: 112 tones, 257–6350 Hz, 24/octave, amplitude 0.03. Remounting put the capsule the preset calls +90 (811-1897) physically at the bottom; **cabling was untouched, so the preset is right about WHICH capsule and wrong about WHERE** — `read_map` honours a `labels_mirrored` flag in `meta.json` and negates the labels there, because a map read with the wrong sign is a silent, plausible-looking error. **(a) The reorientation broke the flip's undividable tie.** `X` travels with the arc, the room does not, so the horizontal and vertical symmetric parts share only `X_sym`: cov = **−0.087 dB², 95 % CI [−0.217, +0.025]** (frequency-block bootstrap, tones as the unit) → `X_sym` is consistent with **zero, |X_sym| ≤ 0.16 dB**. The 0.84 dB shared term from the flip is therefore **essentially all room**. `X_anti` does look to survive the move — regressing the vertical map's antisymmetric part on it gives slope **+0.70, CI [+0.06, +1.49]** — consistent with 1, excluding 0 only just, and the band-by-band slopes scatter 0.43–2.70, so treat it as suggestive. **Bottom line: ~0.2 dB of the position map is arc hardware and the rest is the room.** **(b) The biggest feature in the vertical map is the loudspeaker.** Spread 4.76 dB (vs 3.86 horizontal) with a violent narrowband block at **4238–4490 Hz (15.6 dB at 4362)**, bottom third hot and top third cold. Three source manipulations, each ~90 s at `--fmin 3400 --fmax 5600`: moving the sphere back **20 cm** (2.5 wavelengths of path at 4362 Hz) left it at 16.3 dB, and **reversing its aim** left it at 24.3 dB — both kept the same top/bottom sign, which looked conclusively like room or arc. Then **spinning it 90° about its own aim axis, moving no hardware but the driver's roll, erased it**: r(before, after) = **+0.014**, spread 24.3 → **6.2 dB**, and the −19.4 dB null at +90° gone. A room or arc effect cannot be touched by rolling the source. So at 3.4–5.6 kHz the eleven capsules are measuring **the driver's own angular radiation pattern** — an 8 cm cone in breakup, with real angular order, not just beaming. **Consequences:** the sphere is unusable as an omni reference above ~3 kHz and every high-band number taken with it is contaminated; the flip analysis survives because the source never moved there, so its pattern sits in the room term (read "room **and source**"); the fix, if the high band is wanted, is to capture at 0/90/180/270° of source roll and average, which cancels the angular-order components. Runs `vertical`, `vertical-source-back`, `rotated-zoom` (aim reversed), `rotated90-zoom` in `calibrator/sessions/2026-09-17/`, mirrored to the data repo; figures `source-tests-quad.png` (the four side by side) and `source-move-pair.png`. **Two operational gotchas cost 40 minutes:** a `play_tone` process left holding the PCM2902 makes the device look **absent** to PortAudio, so `acquire_speaker` blames PipeWire and you chase the wrong thing (check `fuser /dev/snd/pcmC12D0p` first); and a power-cycle of the amp hands the card back to PipeWire, which re-grabs it *and* resets `PCM Playback Volume` to −6 dB — the gate catches the volume, nothing catches the grab.
- **The five-cluster degeneracy is RESOLVED (2026-09-17).** The bench substitution measured every capsule, so `arc_error_map.py --no-mic-term` and `arc_validation_diagnostics.py --no-mic-term` now fit `level = run gain + position` with no microphone block: one overall offset instead of five undetermined directions, and the position map is determined. Acceptance: dropping eleven free parameters costs 0.14 dB of residual (1.43 → 1.56 dB). The published maps, the data pack and the figures were regenerated on this fit; `scripts/build_arc_data_pack.py` regenerates `docs/analysis/arc-validation-data.json` and carries a `--verify-against` mode that reproduces the pre-2026-09-17 pack exactly (0.000 dB on all 21 columns) when given the factory cal files and the mic term — run it before trusting any regeneration. `scripts/build_brief_fig2.py` rebuilds the one-pager's Fig. 2, whose left panel is now the bench correction rather than a fitted mic term. **What moved:** −36° vs +36° at 250 Hz 12.5 → **11.7 dB**; largest tone-notched contrast −72°/+54° at 397 Hz 4.3 → **2.9 dB** (largest anywhere is now +72° vs +18° at 2 kHz, 6.5 dB); broadband floor **84 %** of cells within ±1.3 dB, worst **3.3 dB** (was 92 % / 2.7) — the floor looks *worse* because error the old fit charged to microphones now sits on positions; ISO-3745-analogy failures 29 → **40** broadband and 55 → **72** mixed-band of 209. The paragraph below is kept because it is the diagnosis that made the bench session necessary.
- **The position map is only determined within five clusters (found 2026-09-11, remedies report Problem 6).** The fit `level = run gain + position + microphone` has 34 parameters per band on 132 observations and the twelve core runs determine only 28. Two missing directions are the harmless overall offsets; the other four are real, because the position/serial graph splits into five disconnected groups (±90; −72/−54/+54/+72; ±36; ±18; 0) and no microphone ever sat in two of them. Verified on the data: at 794 Hz, +1.0 dB on the ±36 positions and −1.0 dB on their two serials leaves the residual RMS identical at 1.5012 dB. `arc_error_map.py` uses `lstsq`, so the split it reports is the minimum-norm convention. **Contrasts inside a group hold** (including the one-pager's biggest feature, −36° vs +36° at 250 Hz, 12.5 dB apart in the mixed band, and the largest tone-notched contrast, −72° vs +54° at 397 Hz, 4.3 dB apart, and the whole speed-dependence result, since the grouping is identical at every speed); **comparisons across groups, and every position against the arc mean, carry an unmeasured offset of order 1 dB**. The fix is substitution calibration of the eleven capsules against each other (IEC 61094-8; Garg et al. 2019 report ±0.36–0.52 dB over 125 Hz–20 kHz): with M known the design has one undetermined direction instead of five and a *single capture* determines the whole position map, which makes the planned 11-run cyclic rotation unnecessary as evidence. The reference is **one of our own UMIK capsules, not the Norsonic** (decided 2026-09-11): the field-cancels-at-one-seat argument is exact only between identical bodies, so the eleven are evened out USB to USB first and a Class 1 instrument is used later only as the absolute anchor. Pick any capsule except 811-1892 and keep it as the reference forever. The result needs no code: fold each measured difference into that serial's `cal.gain_db` curve, which the pipeline already subtracts. Conditions: one mechanical seat so every capsule returns to the same point and orientation, one capsule left in place as a drift monitor, analog gain locked, a sweep rather than stepped tones, repeated at 2–3 points. Written up in `docs/arc-validation-remedies.pdf` §00/Problems 4 and 6 and §15 session 2b, `docs/chamber-fighting-guide.pdf` §03 method 3, and the one-pager's method panel. **Session-2b capture tooling (2026-09-15):** `calibrator/` — a dead-simple single-mic helper library (run from repo root with `.venv/bin/python`: `acquire_umik` / `acquire_speaker` / `assert_mic_ok` / `assert_speaker_ok` / `take_sample_at_frequency(m, s, freq)` / `take_octave_series` (octave steps 200 Hz→15 kHz) / `assert_tone_stable(sample, freq)` (per-capture gate: tone present ≥10 dB above the adjacent floor, level and frequency steady across 0.25 s chunks — the old ring-buffer playback fails it, the inline-callback path passes with <2 dB dev) / `save_wav` / **full-curve session layer:** `capture_capsule(m, s, label, out_dir)` (one call per physical swap: preferred 1/3-octave 63 Hz–16 kHz, 6 s captures below 250 Hz, per-freq gate failures recorded and skipped, not fatal; repeat label = `__2` suffix = the reference bracket), `relative_curves(session_dir, reference_label)` (diff vs reference, bracket runs averaged, drift reported), `merge_curve(factory_path, freqs, diff_db, out_path)` (folds the difference into the serial's factory REW file: gain += diff, Sens Factor kept verbatim until a real 94 dB calibrator anchors the reference); no serials, no tests by design — identity = which mic is plugged in, exactly-one rule, stepped tones = a caller loop over freqs; `acquire_speaker` is pinned to the PCM2902 by USB ID `08bb:2902`, so other USB audio devices plugged in don't break it). **Thin CLI (`calibrator/cli.py`, `python -m calibrator …`):** `devices` (acquire + gates), `capture <label> [--out DIR] [--freqs …] [--per-octave N] [--fmin Hz] [--fmax Hz] [--amplitude A]` (one capsule per invocation; default session dir `calibrator/sessions/<today>`; `--per-octave` switches to the denser IEC-61260 log grid `log_series()` — 6 = 49 points ≈ 3 min/capsule, 12 = 97 points ≈ 6 min; `--fmin/--fmax` bound either grid; **one grid per session**, postprocessing matches exact frequency keys), `process --ref <label> [--dir DIR]` (prints per-capsule diff range + reference-bracket drift warning, writes `<label>_relative.txt` REW curves) — wiring only, no logic; verified end-to-end 2026-09-15 with two untouched-rig captures agreeing within 0.05–0.17 dB per frequency. Both USB mixers are device-firmware state that WirePlumber can rewrite, so both are gated at 0 dB before *every* capture: the UMIK's `Mic Capture Volume` (0…−50 dB) and the PCM2902 interface's `PCM Playback Volume` (0…−128 dB — **it shipped at −23 dB**; one-time fix `amixer -c 1 cset numid=4,iface=MIXER 128`). On the laptop WirePlumber claims both USB cards and holds their capture streams open, which makes them vanish from PortAudio entirely — `wpctl set-profile <dev> off` (device 88 = PCM2902, 107 = UMIK-2 at the time) before any session; the acquire errors say so. **Playback is generated inline in the PortAudio callback** — `sd.play(loop=True)` with its ring-buffer thread dropped 2–22 ms bursts 20–80× per capture behind the dock's full-speed hub (host underflows happened downstream of PortAudio); the inline generator measures 0 underflows, tone SNR 54–98 dB, and a 30 ms capture head is trimmed off as insurance. Drift/monitor/bracket protocol is deliberately absent: whatever gets captured is what postprocessing works with. The speaker→amp→speaker analog chain is fixed-gain by Adam (pots removed).
- **Tones and broadband are reported separately (2026-09-09, issue #12).** The stand has no RPM channel, so `server/core/tones.py` reads the blade-passage frequency from each capture's spectrum (most prominent peak in 100–320 Hz with its 2nd and 3rd harmonics present; peaks are found over 50–640 Hz and then restricted to 100–320 Hz, so a fundamental near either bound keeps its full prominence), stores it as `bpf_hz` in the acoustic `meta.json`, and the FFT endpoint returns per-harmonic tone levels (±3 bins) plus third-octave levels with every multiple of BPF/2 notched (on a 16384-point Welch so the 125–250 Hz bands keep bins). `pwm_points` flags a capture `off_speed` when its BPF is >3 % off the PWM's median (prop7). The Polar tab's level source (mixed band / BPF harmonic / broadband) exists because the arc validation showed the room bends the tones by up to 8 dB while the broadband polar stays round; a polar drawn from the mixed default band hides that. Both that selector and the BPF readout beside it carry an `InfoToggle` (`LEVEL_SOURCE_HELP` in `src/content/parameterHelp.ts`, added 2026-09-14) explaining what each level source is, why the distinction exists, that the broadband curve is the trustworthy one and tone levels below ~1 kHz are provisional, and why the tone buttons grey out when no BPF was detected. **Trigger sync (issue #13):** `align_captures` only aligns a genuine onset (all mics triggered, none in block 0, spread ≤ 0.1 s); a source already running when the recording starts passes through untouched — it used to crop every mic to the tail after the latest trigger, which is what shortened nine PWM-1800 captures to 0.16–1.08 s.
- **UI wording (2026-09-11, issues #14–#20):** the app opens on an **Intro** page (`src/pages/IntroPage.tsx`; prose and the grant/funding block in `src/content/projectInfo.ts` — the funding fields are placeholders until Adam supplies the programme, title and grant number). Stand controls use RCbenchmark's words: **Tare** (not "zero stand"), **ESC signal (µs)** for the throttle pulse (the data model keeps `pwm_us`), **safety cutoffs**; a measurement is a **capture**, never a "run". The `motor__propeller__shroud__notes` group is a **base** in the UI (API paths and `keySlug` identifiers unchanged). Every **numeric** parameter carries a ⓘ `InfoToggle` whose text lives in `src/content/parameterHelp.ts` (identity fields — mic serial/device rows, the calibration library, presets, the research-tree node picker and the base key fields — do not, and do not need to). The polar plot (`PolarPlotFrame`) has radial-axis drag zoom, a Reset-view button, a zoom hint and a side-view `MicArcSchematic` of the configured mic arc. **Its orientation is pinned (2026-09-14).** Plotly lets a drag on the polar rim rotate the whole subplot and a drag on the radial axis swing that axis to a new angle; either one breaks the plot's meaning here, because elevation is mapped to the clock face (+90° up, 0° right, −90° down) and the radial tick labels render upside down once the axis passes the vertical — which is what Adam saw. `PlotlyChart`'s `pinPolarOrientation` prop listens on `plotly_relayout` and snaps `polar.radialaxis.angle` and `polar.angularaxis.rotation` back to 0, leaving radial-range zoom alone. Also: **Plotly has no wheel zoom on polar subplots at all** (cartesian/gl3d/geo/map only), so the old `scrollZoom: true` on the polar config did nothing and the hint text that promised it was wrong. **Base labels (2026-09-14):** both base pickers render through `src/components/results/keyLabel.ts`, because they had drifted apart and the overlay picker was `w-56` — wide enough for "2004 · 6in" and nothing else, so every horizontal-arc base looked identical (reported by Przemysław Kornatowski, 2026-09-13). It is now `min-w-[24rem]`, the same as `KeyPicker`. The marks are explained by an `InfoToggle` popup on both Base labels (`BASE_HELP.arcMark` in `src/content/parameterHelp.ts`) rather than a standing legend line. The label carries an arc mark from `src/content/arcOrientation.ts`, because the store holds two different rig geometries and nothing in the capture path records which: **no mark** = arc standing normally (the dp1 material study, where the mics really are at different elevations); **[H]** = arc laid flat for the validation campaign, reference orientation (prop7–10, prop15–18); **[H F]** = laid flat and turned 180°, so every elevation label is mirrored (prop11–14); **[H ?]** = laid flat but the orientation is unknown (the six 31 August captures and `dp1-rubber-horizontal`). **prop1–prop6 are normal horizontal on Adam's own account of the session (2026-09-14)**, which overrides the fit: `detect_rotated()` places only the twelve coherent runs and found prop1–6 consistent with neither orientation. That leaves an open question rather than a closed one — if the arc was standing the same way, something else differed in that morning session (prop1 11:55 → prop6 13:13, then a 3.5 h gap to prop7 at 16:42), cause unknown, so they stay out of the maps until it is found. The flat campaign is recognised by `horizontal` in the notes; the turn was recovered afterwards by `detect_rotated()` in `scripts/arc_error_map.py` and lives as `runs[*].flipped` in `docs/analysis/arc-validation-data.json`, agreeing with the one-pager ("prop11–14 were measured with the arc turned around… correlation +0.79 to +0.98"). The *confirmed* sets are the ones hardcoded, so a flat-arc capture added later reads as unknown rather than silently looking confirmed.
- **FFT defaults:** Hann window, 4096 size, 50% overlap, log-x 20 Hz–24 kHz, dBSPL after UMIK calibration applied (dBFS fallback if no cal file uploaded). Welch PSD via `scipy.signal.welch`.
- **Calibration signs — the curve is SUBTRACTED, the Sens Factor is ADDED.** They are different quantities. `cal.gain_db` is the mic's *own gain response* (its coloration), not a correction: [REW](https://www.roomeqwizard.com/help/help_en-GB/html/calfiles.html), whose format this is, says it "should contain the actual gain … response of the meter or microphone … these will then be **subtracted** from subsequent measurements". miniDSP say the same. The curve's shape corroborates it — ~0 dB at 1 kHz, negative at both extremes, i.e. a capsule rolling off; a *correction* would be positive there. **We had this backwards until 2026-09-02**, which doubled the curve instead of removing it: ~0.6 dB mean error but **3.7 dB of fabricated spread across the 11-mic set**, landing directly on directivity. Pinned by `test_curve_and_sens_factor_have_opposite_signs`. Spectra are computed on read, so existing measurements corrected themselves — no re-capture.
- **Absolute dB SPL conversion:** `SPL = level_dBFS - sens_factor_db + 94`. The UMIK Sens Factor is the dBFS the mic reports when driven by a 94 dB SPL (1 Pa) calibrator, quoted under REW's convention that a full-scale sine is **−3.01 dBFS**, not 0 — which our Welch `scaling="density"` + band integration already reproduces, so no ±3.01 fudge anywhere. **AGain is deliberately not added**: the Sens Factor already accounts for the mic's internal gain, AGain only records which analog-gain setting it is valid at. A cal file with a response curve but **no** Sens Factor cannot give absolute levels — `FFTResponse.calibrated` then stays `true` (cal applied) while `absolute_spl` is `false`, and the UI labels those axes `dBFS`, never `dB SPL`.
- **Psychoacoustics calibration (Pa scaling):** mosqito wants Pascals, so the time signal is multiplied by `calibration.pascals_per_full_scale(cal)` = `20e-6 * 10**((94 - sens_factor_db)/20)` before `loudness_zwst`/`roughness_dw` see it — the same Sens Factor convention as the FFT path (a full-scale sine lands on the identical SPL either way; asserted in `test_calibration.py`). `compute_metrics(..., pa_per_full_scale=...)` is keyword-only with **no default** so nobody gets arbitrary-scale sone by accident; `None` sets `PsychoacousticMetrics.absolute = false` and the Results tab tags those rows `rel`, refusing to colour-rank them against calibrated rows (uncalibrated loudness error is nonlinear, not an offset). **The per-frequency response curve is deliberately NOT applied here** — that needs an FIR filter on the time signal; scalar only. Cached `psychoacoustics.json` files carry `version` (`store/psychoacoustics.CACHE_VERSION`); anything older/unversioned is a cache miss and gets recomputed.
- **Sample rate:** 48 kHz (RPi 4 USB bandwidth makes 96 kHz unsafe with 6 mics; sufficient for drone-noise band 100 Hz–20 kHz).
- **PWM step model:** each step carries its own `recording_ms` (the audio capture duration at that PWM). Total recording per half is `sum(step.recording_ms)`. Stabilization time is automatic (server `stabilize_rpm` waits for RPM to settle within tolerance).

## Tech stack

**Server (`server/`):** Python 3.12 + FastAPI + asyncio. `sounddevice` (PortAudio) for UMIK-2 capture · `pyserial-asyncio` for Tyto MSP · `aioftp` + `websockets` for Norsonic (later) · `numpy` + `scipy` for FFT/Welch/calibration · `aiofiles` for the measurement store · `mosqito` for psychoacoustic SQM/PA computation.

**Client (`src/`):** React 19 + Vite 7 + TypeScript + Tailwind 4 + Zustand 5 + Plotly.js.

**Third-party attribution:** Psychoacoustic metrics are computed with [MOSQITO](https://github.com/Eomys/MoSQITo) (Green Forge Coop, BSD). When publishing results derived from those metrics, cite: *Green Forge Coop. MOSQITO [Computer software]. https://doi.org/10.5281/zenodo.5284054* (use GitHub's "Cite this repository" button for the release-pinned form).

## Conventions

- Don't add comments unless the *why* is non-obvious. No docstring bloat.
- Don't pre-build for hypothetical features. MVP target is laptop + UMIKs + Tyto; Norsonic and RPi packaging are deferred phases — don't write code for them now.
- `server/vendor/pawel/` is treated as upstream — modifications limited to package-relative import fixes (see `server/vendor/pawel/README.md`). Calibration overrides happen in `server/core/calibration_override.py`, not in the vendored module.
- Server-side trigger-onset sync is the chosen mic-alignment strategy — UMIK-2s cannot be hardware-clock-locked.
- For UI changes, dev server is `npm run dev` (frontend) + `SOUNDVIS_DATA=../SoundVisualizer-data/data uvicorn server.main:app --reload` (backend on `:8000`). Never start the backend without `SOUNDVIS_DATA` — see *Running it*.
- The `/dev/seed` and `/dev/fake_capture` endpoints synthesize realistic propeller noise (BPF + harmonics + LF spreaded + HF broadband, with elevation-dependent directivity) so the Results tools can be developed and demoed without the rig.

## Maintenance — standing instructions

The user has standing approval to do the following at natural break points (e.g. after finishing a phase, fixing a bug that crossed several files, adding a feature, or whenever docs and code drift):

**1. Keep the following five files coherent with the current state of the code:**

| File | Should reflect |
|------|----------------|
| `CLAUDE.md` (this file) | Status banner, hardware availability, locked decisions, conventions, **anything you wish a fresh Claude session would know** |
| `PLAN.md` | Phase table with ✅/⏳ status, post-MVP enhancements list, open questions |
| `README.md` | What the project is, page surfaces, quick start, real-hardware bring-up, data layout, attribution |
| `server/README.md` | Module layout, route inventory, conventions, test count |
| `docs/overview.html` | Status pill, phase badges, hardware availability table |

When code lands that adds a feature, changes a schema, or completes/defers a phase, also update whichever of the five docs above is now stale. The user does *not* want to be asked each time — just do it as part of the same edit batch.

**2. Commit + push to GitHub.** The remote is `origin → https://github.com/asdfgh0318/SoundVisualizer.git`. After a coherent chunk of work + updated docs:
- Stage the specific files (not blanket `git add .` — review what changed first).
- Write a concise commit message describing the *why*, not just *what*.
- `git push origin main` to the remote.
- Skip commits when the change is purely exploratory (no code/doc change), or when the working tree is already pristine.

The standard Anthropic safety rules still apply: don't commit secrets, don't `--no-verify`, don't force-push to `main`, don't amend the initial commit. Treat `data/` and `config.toml` as gitignored (already configured).

**3. Track future work as GitHub issues.** Before starting non-trivial new work (a feature, a bug fix that spans files, a phase, an experiment), create a GitHub issue describing it:
- `gh issue create --title "..." --body "..."` (auth via `gh auth status`).
- One issue per logical unit. Include scope, what's in/out, and acceptance criteria.
- Reference the issue in the relevant commit messages (`closes #N`) so GitHub auto-links them and closes the issue on merge to `main`.
- Quick bug fixes, doc tweaks, and small one-off changes don't need an issue.

**4. Rebuild the demo Docker image after every code-touching commit.** The demo image bakes the React bundle in at build time (`COPY --from=frontend-build /app/dist /app/static`), so old images serve stale UI. After pushing a commit that touches `src/`, `server/`, `package*.json`, `pyproject.toml`, `index.html`, `vite.config.ts`, `tsconfig*.json`, `Dockerfile`, or `config.example.toml`:
- `sudo docker compose build` (rebuilds Stage 1 / Stage 2 with current source).
- Verify the rebuild succeeded; you don't need to start the container unless the user asks.
- Skip the rebuild for docs-only commits (CLAUDE.md, PLAN.md, README.md, docs/), commits touching only `.gitignore` / `.dockerignore` exclusions, or test-only commits in `server/tests/` that don't change runtime behaviour.
- The image is local-only (no registry push). Anyone cloning the repo gets a fresh build on their first `docker compose up --build`, so this step is for *your* local demo to stay current — not a release artifact.

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Python
__pycache__/
*.py[cod]
*$py.class
.venv/
venv/
.env
*.egg-info/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Captured data — never commit measurements
/data/

# Per-host config (calibration constants for the specific Tyto unit, TTY paths, etc.)
/config.toml

# Reference PDFs — research papers we read while building the project. Useful
# locally but not worth bloating the repo. The two generated reports in docs/
# (psychoacoustics_report{,_pl}.pdf) are tracked separately. Recursive so
# topic subfolders (shroud_comparison/, etc.) are also ignored.
docs/references/**/*.pdf
